//BATCH 12
//TEAM MEMBERS-NIKITA BHALLA-2011A7PS122P
//POOJA GARG-2011A7PS056P
typedef char buffer[256];
typedef int buffersize;
//enum Type{INT, MATRIX, STRING};
#define SymbolTableSize 1000
#define sizeBuffer 100000
typedef struct token{
	char lexeme[256];
	int tokenIndex;
	int lineNumber;	
	struct token* next;	
}token;
typedef token* tokenInfo;
typedef struct tokenQueue{tokenInfo Result; tokenInfo Last;}tokenQueue;


